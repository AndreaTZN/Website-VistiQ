# pnpm 10 treats this file as a workspace declaration and errors out without
# `packages`; the repo is a single package at the root.
packages:
  - "."
allowBuilds:
  "@parcel/watcher": true
  esbuild: true
  sharp: true
